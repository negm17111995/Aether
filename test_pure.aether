// Test: Pure Aether Hello World
// Compile with: clang /tmp/start.o test_pure.s -o test_pure -lSystem

// Import pure runtime
import std.native.runtime

func main() -> Int {
    // Test heap allocation
    let buf = malloc(100)
    
    // Test write
    let msg = "Hello from Pure Aether!\n"
    sys_write(1, msg, 24)
    
    // Return success
    0
}
