// Aether Benchmark - Fibonacci
// Returns the result as exit code (will be modulo 256)

func fib(n: Int) -> Int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

func main() -> Int {
    let result = fib(35)
    return result
}
