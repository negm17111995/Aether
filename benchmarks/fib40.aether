// Aether Benchmark - Fibonacci(40)
// Result: 102334155
// Mod 256: 75

func fib(n: Int) -> Int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

func main() -> Int {
    let result = fib(40)
    return result
}
