// Runner for Extreme Tak
import std.native.runtime

// Dummy implementation (will be replaced by ASM)
func tak_extreme(x: Int, y: Int, z: Int) -> Int {
    // This body will be deleted from assembly
    if y >= x {
        z
    } else {
        tak_extreme(x - 1, y, z)
    }
}

func main() -> Int {
    // tak(30, 20, 10)
    tak_extreme(30, 20, 10)
}
