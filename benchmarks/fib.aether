// Aether Benchmark - Fibonacci
// Tests: recursion, arithmetic, function calls

func fib(n: Int) -> Int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

func main() {
    let start = 0
    let n = 35
    let result = fib(n)
    println(result)
}
