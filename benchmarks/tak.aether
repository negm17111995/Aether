func tak(x: Int, y: Int, z: Int) -> Int {
    if y < x {
        return tak(tak(x - 1, y, z), tak(y - 1, z, x), tak(z - 1, x, y))
    }
    return z
}

func main() -> Int {
    return tak(18, 12, 6)
}
