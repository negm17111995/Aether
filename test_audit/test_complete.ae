// TEST: Complete System
import std
import std
import std.core
import std.core.result
import std.runtime.safety
import std.runtime.security

func main() -> Int {
    // Test std
    print(ae_abs(0 - 10))
    
    // Test core
    print(math_min(10, 20))
    
    // Test result
    let r = Ok(42)
    print(unwrap(r))
    
    // Test safety
    let p = safe_alloc(16)
    safe_free(p)
    print(100)
    
    // Test security
    let x = secure_random_int()
    // x will be 0 due to stub, but ensures it calls
    print(x)
    
    0
}
