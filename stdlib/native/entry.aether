// AETHER NATIVE ENTRY - Bootstrap Compatible
// Entry point for native execution

import std

func aether_native_main(argc: Int, argv: Int, envp: Int) -> Int {
    0
}

func heap_alloc(size: Int) -> Int {
    ae_malloc(size)
}

func heap_free(ptr: Int) {
    __builtin_free(ptr)
}

func native_init_globals() {
    // Initialize global state
}
