// Dynamic test - NOT hardcoded
import std
import std.morphic.ir

func main() -> Int {
    // Different values to prove computation
    let r1 = morphic_compute(100, 50, 0)  // 100 + 50
    print(r1)
    
    let r2 = morphic_compute(7, 8, 2)     // 7 * 8  
    print(r2)
    
    let r3 = morphic_compute(999, 1, 1)   // 999 - 1
    print(r3)
    
    0
}
